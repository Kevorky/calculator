export enum Operand {
  Add = '+',
  Subtract = '-',
  Multiply = '*',
  Divide = '/',
  None = '',
}

export enum Action {
  AC = 'AC',
  PlusMinus = 'PlusMinus',
  Percent = 'Percent',
  Dot = 'Dot',
  Equal = 'Equal',
}
